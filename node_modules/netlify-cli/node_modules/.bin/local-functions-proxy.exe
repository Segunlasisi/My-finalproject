#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

"$basedir/../@netlify/local-functions-proxy-win32-ia32/bin/local-functions-proxy.exe"   "$@"
exit $?
